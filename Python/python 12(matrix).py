# Matrix
matrix=[
      [1,2,3],
      [4,5,6],

]
for row in matrix:
    for col in row:
        print (col)
        
        
        
print (matrix[0] [2])  #for showing specific element


matrix [0][1]=10       #changing value in a specific index
print (matrix[0] [1])
